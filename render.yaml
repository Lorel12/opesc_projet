services:
  - type: web
    name: mon-app-flask
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: gunicorn app:app --workers 4 --timeout 60 --bind 0.0.0.0:10000
    envVars:
      - key: FLASK_ENV
        value: production
